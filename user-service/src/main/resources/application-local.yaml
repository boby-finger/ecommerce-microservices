security:
  jwt:
    jwk-set-uri: ${AUTH_JWKS_URI:http://localhost:8002/oauth2/jwks}

spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}

  jpa:
    show-sql: true